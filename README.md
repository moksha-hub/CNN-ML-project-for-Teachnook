## Project Overview
This project uses a Convolutional Neural Network (CNN) built with TensorFlow/Keras to classify images from the CIFAR-10 dataset into 10 different categories. The model is trained on labeled images and evaluated using test accuracy and a classification report.
